// lib/logic/game_logic.dart
import '../core/workout_data.dart';

class GameLogic {
  void addExpToUser(WorkoutType type) {
    print("BE1: ${type}에 대한 경험치를 계산합니다.");
    // 여기에 이준식 님이 실제 로직을 짤 예정입니다.
  }
}
